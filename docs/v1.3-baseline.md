# v1.3 — Phase 0 baseline (measured on v1.2.0)

Measured on branch `feat/v1.3-real-profit` at its fork point from `main`
(commit `348f4b6`, "feat: v1.2.0 …"), Node 22.22.2, `npm ci` from the committed lockfile.

## Commands

```
npm run typecheck   # clean
npm test            # 5 files, 71 tests, all passing
npm run build       # 486 modules, ~3s
```

## Bundle sizes

Gzip measured with `gzip -9 -c <file> | wc -c` (the number the budget script uses).
Vite's own report is quoted alongside because it uses a different gzip level.

| Artifact | raw bytes | gzip bytes (`-9`) | vite report |
| --- | --- | --- | --- |
| **`assets/index-*.js` (entry)** | **376,067** | **122,581** | 123.28 kB |
| `index.html` (CSS inlined) | 40,794 | 9,776 | 9.86 kB |
| `assets/db-*.js` (Dexie, lazy) | 97,353 | 32,691 | 32.80 kB |
| `assets/HistorySheet-*.js` | 6,088 | 2,379 | 2.35 kB |
| `assets/BasketSheet-*.js` | 6,772 | 2,316 | 2.29 kB |
| `assets/SettingsSheet-*.js` | 4,658 | 1,459 | 1.43 kB |
| `assets/workbox-window.*.js` | 5,748 | 2,395 | 2.36 kB |
| `assets/dexie-react-hooks-*.js` | 1,304 | 620 | 0.59 kB |
| `sw.js` | 3,025 | 1,255 | — |
| `workbox-*.js` | 15,112 | 5,228 | — |

## Precache

`generateSW` — **33 entries, 1335.48 KiB**.
`globPatterns: ['**/*.{js,css,html,svg,png,woff2,webmanifest}']` — note `json` is **absent**,
so `src/data/inflation.json` must be *statically imported* (bundled into JS), never fetched.

## Budget for v1.3

* **Entry JS gzip ceiling: 125,653 bytes** (122,581 + 3 × 1024).
  Enforced in CI by `scripts/bundle-budget.mjs`.
* Every new surface (installment schedule, products view, backup, What's New) must be a
  separate lazy chunk, not part of the entry.

## Test baseline

| file | tests |
| --- | --- |
| `src/lib/calc.test.ts` | 27 |
| `src/lib/numbers.test.ts` | 21 |
| `src/lib/share.test.ts` | 10 |
| `src/lib/units.test.ts` | 7 |
| `src/lib/basket.test.ts` | 6 |
| **total** | **71** |

## Environment facts verified

* `vite-plugin-pwa@0.21.2` does expose `RegisterSWOptions.onRegisteredSW(swScriptUrl, registration)`
  (`node_modules/vite-plugin-pwa/types/index.d.ts:18`) — Feature D can rely on it.
* Chromium for Playwright: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`
  (already hard-coded in `scripts/screenshots.mjs` and `scripts/verify-offline.mjs`).
* There is **no existing Jalali date utility** in the repo, contrary to the brief.
  `HistorySheet` gets Jalali dates for free from `Intl.DateTimeFormat('fa-IR')`, whose default
  calendar is `persian`. v1.3 adds `src/lib/dates.ts` wrapping exactly that — no new dependency.
